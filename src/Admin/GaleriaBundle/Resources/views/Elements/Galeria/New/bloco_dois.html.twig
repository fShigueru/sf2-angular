<div class="x_panel" ng-controller="NewCtrl">
    <div class="x_title">
        <h2>{{ 'title.galeria.vincular.aonde'|transchoice(0,{}, 'messagesGaleriaBundle') }}</h2>
        <i class="fa fa-question red padding-left-7 padding-top-8 cursor-pointer" data-toggle="modal" data-target=".modal-duvida-galeria-vincular"></i>
        <ul class="nav navbar-right panel_toolbox">
            <li>
                <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            <li class="dropdown"></li>
            </li>
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <div class="row">
            <div class="form-horizontal form-label-left">
                <div class="col-md-12 col-sm-12 col-xs-12 form-group">
                    <div class="form-group {{ form.entity.vars.required ? 'item' : '' }}">
                        <label class="control-label col-md-3 col-sm-3 col-xs-12" for="{{ form.entity.vars.id }}">
                            {{ form.entity.vars.label|transchoice(0,{}, 'messagesGaleriaBundle') }}
                            {{ form.entity.vars.required ? '<span class="required">*</span>' : '' }}
                        </label>
                        <div class="col-md-6 col-sm-6 col-xs-12">
                            {{ form_row(form.entity, { 'label': false}) }}
                        </div>
                    </div>
                </div>
                <div class="col-md-12 col-sm-12 col-xs-12 form-group" ng-hide="idEntity">
                    <div class="form-group {{ formIdEntity.idEntity.vars.required ? 'item' : '' }}">
                        <label class="control-label col-md-3 col-sm-3 col-xs-12" for="{{ formIdEntity.idEntity.vars.id }}">
                            {{ formIdEntity.idEntity.vars.label|transchoice(0,{}, 'messagesGaleriaBundle') }}
                            {{ formIdEntity.idEntity.vars.required ? '<span class="required">*</span>' : '' }}
                        </label>
                        <div class="col-md-6 col-sm-6 col-xs-12">
                            {{ form_row(formIdEntity.idEntity, { 'label': false }) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>